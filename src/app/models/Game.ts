export interface Game {
  id: number;
  providerId: number;
  name: string;
  providerName: string;
  storyLine: string;
  summary: string;
  coverUrl: string;
  coverHeight: number;
  coverWidth: number;
}
